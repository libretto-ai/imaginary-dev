{
  "name": "transformer-shared-tests",
  "version": "0.0.1",
  "description": "Shared tests for imaginary transformers",
  "scripts": {
    "clean": "rm -rf dist/",
    "full-clean": "npm run clean && rm -rf node_modules"
  },
  "author": "Sasha Aickin",
  "license": "UNLICENSED",
  "dependencies": {
    "@microsoft/tsdoc": "^0.14.2"
  },
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@types/jest": "^29.5.6",
    "ajv": "^8.12.0",
    "json-schema-diff": "^0.18.0"
  }
}
