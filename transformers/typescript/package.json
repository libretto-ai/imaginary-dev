{
  "name": "@imaginary-dev/typescript-transformer",
  "version": "0.0.6",
  "description": "A TypeScript plugin for transforming imaginary functions",
  "files": [
    "dist"
  ],
  "main": "dist/transformers/typescript/src/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist/",
    "full-clean": "npm run clean && rm -rf node_modules",
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "author": "Sasha Aickin",
  "license": "MIT",
  "dependencies": {
    "@imaginary-dev/util": "*",
    "@microsoft/tsdoc": "^0.14.2",
    "ajv": "^8.12.0",
    "@imaginary-dev/util": "*"
  },
  "devDependencies": {
    "transformer-shared-tests": "*",
    "ts-jest": "^29.0.5"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
